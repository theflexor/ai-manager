import { LoginPage } from '@/pages/login.page';
import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Login - AI Subscription Manager',
  description: 'Login to your account',
};

export default LoginPage;

